namespace Ets.Domain.Enums;

/// <summary>
/// 應變人員角色列舉。尚未使用!
/// 對應 EventResponders.Role 欄位字串值。
/// </summary>
public enum ResponderRole
{
    Commander,
    Mgr,
    ViceMgr,
    Normal,
    Contacts,
    Observer
}
